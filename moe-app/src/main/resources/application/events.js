var MOE = MOE || {};

MOE.Events = {
    Core: {
        Controls: {
            Validation: {
                REQUEST_REMOTE_VALIDATION: "requestRemoteValidationEvent"
            }
        }
    }
};

