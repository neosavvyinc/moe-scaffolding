'use strict';

describe(' Send to Trader', function () {

    xit(' Should have a checkbox that informs the system to send instructions to trader', function () {

        fail('unimplemented');
    });

    xit(' Should have a field to support entry for instructions', function () {

        fail('unimplemented');
    });

    xit(' Should have a max number of 20', function () {

        fail('unimplemented');
    });

    xit(' Should have a field that is non-editable if the checkbox is not selected', function () {

        fail('unimplemented');
    });

    xit(' Should have a required entry of data in the instructions if yes is selected', function () {

        fail('unimplemented');
    });
});