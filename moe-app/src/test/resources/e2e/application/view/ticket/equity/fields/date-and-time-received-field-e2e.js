'use strict';

describe(' Date and Time Received (Label should be Order Received By) (UX Update)', function () {

    xit(' Should have a checkbox with Now as the label and if it is selected the other two fields are disabled', function () {

        fail('unimplemented');
    });

    xit(' Should by default select the checkbox for Now', function () {

        fail('unimplemented');
    });

    xit(' Should have a date input to select the date', function () {

        fail('unimplemented');
    });

    xit(' Should not allow a date in the future be selected for the date received', function () {

        fail('unimplemented');
    });

    xit(' Should have a time input to select a time in EST', function () {

        fail('unimplemented');
    });

    xit(' Should not allow a time in the future be selected', function () {

        fail('unimplemented');
    });
});